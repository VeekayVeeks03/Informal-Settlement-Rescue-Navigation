# Cleanup Report - Navigation

Prepared for GitHub portfolio upload.

## Changes Made

- Renamed the project root from `222141979miniproject` to `Navigation` as requested.
- Removed generated distribution/build output from `dist/`.
- Removed the compiled JAR file `dist/Mini project.jar`.
- Moved sample testing images into `sample-images/`.
- Added a professional `README.md` at the root of the project.
- Added a Java/Eclipse `.gitignore`.
- Kept the source code intact under `src/`.
- Kept the presentation file under `ss/`.

## Notes

This project uses JavaFX. To run it, JavaFX must be configured in the IDE using the module path and JavaFX modules listed in the README.
